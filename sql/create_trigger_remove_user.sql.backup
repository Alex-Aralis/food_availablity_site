USE food_account_data;

DELIMITER //

CREATE OR REPLACE TRIGGER remove_user BEFORE DELETE ON accounts FOR EACH ROW
BEGIN
    DELETE FROM mysql.user WHERE User!='root' AND User!='food_user' AND User=OLD.user_name;
    DELETE FROM mysql.roles_mapping WHERE User!='root' AND User=OLD.user_name;
END//

DELIMTER ;
  
